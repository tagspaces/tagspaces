/**
 * Global setup for unit tests
 * Note: This runs in Node, not in browser context, so it can't set window properties
 * Window mocking needs to happen per-test file
 */

export default function globalSetup() {
  // Placeholder - actual setup happens in each test file
}
